// Shared API surface types between the Bun + Hono backend (apps/api) and the
// React web client (apps/web). When the Swift iOS bridge gets a generated
// client this file's zod schemas will drive it too.
//
// Stays empty until the M3 web rewrite starts pulling concrete endpoints
// across — adding speculative shapes here ahead of consumers just creates
// drift to fix later.

export {};
